import{redirect}from"next/navigation";import{requireAdminCapability}from"@/lib/security/adminAuthorization";import SafetyQueueClient from"./SafetyQueueClient";export const dynamic="force-dynamic";export default async function Page(){const a=await requireAdminCapability("safety.case.read");if(a.ok === false)redirect(a.actionPath?`${a.actionPath}?next=${encodeURIComponent("/admin/safety")}`:"/dashboard");return <SafetyQueueClient/>}
