import Link from "next/link"
import { loadOperatorOnlyFansTerminalHistory } from "@/lib/creator-publishing-queue/onlyfans-history/loaders"
export const metadata = { title: "OnlyFans completed history — Sirens Forge" }
export default async function OnlyFansOperatorTerminalHistoryPage(){ const result=await loadOperatorOnlyFansTerminalHistory(); return <main className="min-h-screen bg-black px-4 py-10 text-white sm:px-6 lg:px-8"><div className="mx-auto max-w-5xl"><Link href="/creator/publishing-queue/operator" className="text-sm text-fuchsia-200 underline">Back to operator queue</Link><p className="mt-6 text-sm uppercase tracking-[0.3em] text-cyan-200">OnlyFans assisted operator</p><h1 className="mt-3 text-4xl font-bold">Completed and terminal history</h1>{!result.ok ? <p className="mt-6 rounded-2xl border border-amber-300/40 bg-amber-950/30 p-5 text-amber-100">{result.message}</p> : result.jobs.length===0 ? <p className="mt-6 rounded-2xl border border-white/10 bg-white/[0.03] p-5 text-zinc-400">No authorized terminal OnlyFans jobs were found.</p> : <ol className="mt-6 space-y-3">{result.jobs.map(job=><li key={job.platformJobId} className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><article className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold">{job.status}</h2><p className="text-sm text-zinc-400">Job {job.platformJobId}</p><time dateTime={job.updatedAt} className="text-sm text-cyan-100">{job.updatedAt}</time></div><Link href={`/creator/publishing-queue/operator/history/${job.platformJobId}`} className="rounded-xl border border-cyan-300/40 px-4 py-2 text-sm font-semibold text-cyan-100 focus:outline-none focus:ring-2 focus:ring-cyan-200">View history</Link></article></li>)}</ol>}</div></main> }
