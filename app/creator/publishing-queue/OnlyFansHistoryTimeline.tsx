import { formatHistoryTimestamp } from "@/lib/creator-publishing-queue/onlyfans-history/timezone"
import type { OnlyFansHistoryView } from "@/lib/creator-publishing-queue/onlyfans-history/types"
export function OnlyFansHistoryTimeline({ view, audience="creator" }: { view: OnlyFansHistoryView; audience?:"creator"|"operator" }) {
  if (view.ok === false) return <section className="mt-8 rounded-3xl border border-amber-300/30 bg-amber-950/20 p-5 text-amber-100" aria-live="polite"><h2 className="text-2xl font-semibold">Publishing history</h2><p className="mt-3">{view.message}</p></section>
  if (view.entries.length === 0) return <section className="mt-8 rounded-3xl border border-white/10 bg-white/[0.04] p-5" aria-live="polite"><h2 className="text-2xl font-semibold">Publishing history</h2><p className="mt-3 text-zinc-400">No OnlyFans publishing history has been recorded yet.</p></section>
  return <section className="mt-8 rounded-3xl border border-white/10 bg-white/[0.04] p-5" aria-labelledby={`${audience}-onlyfans-history-heading`}><div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm uppercase tracking-[0.25em] text-cyan-200">OnlyFans history</p><h2 id={`${audience}-onlyfans-history-heading`} className="text-2xl font-semibold">Publishing timeline</h2></div><p className="text-sm text-zinc-400">Times shown in {view.timezoneLabel}.</p></div><ol className="mt-5 space-y-4">{view.entries.map(entry => <li key={entry.id} className="rounded-2xl border border-white/10 bg-black/30 p-4"><article><div className="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><h3 className="font-semibold text-white">{entry.label}</h3><p className="mt-1 text-sm text-zinc-300">{entry.explanation}</p></div><time dateTime={entry.occurredAt} className="text-sm text-cyan-100">{formatHistoryTimestamp(entry.occurredAt, view.timezone)}</time></div><dl className="mt-3 grid gap-2 text-sm sm:grid-cols-2"><div><dt className="text-zinc-500">Evidence state</dt><dd>{entry.evidenceState ?? "Not applicable"}</dd></div><div><dt className="text-zinc-500">Source</dt><dd>{audience === "operator" ? entry.provenance.replaceAll("_"," ") : entry.provenance === "reconstructed_completion_state" ? "Reconstructed from trusted records" : "Trusted publishing record"}</dd></div>{entry.finalPostUrl ? <div className="sm:col-span-2"><dt className="text-zinc-500">Final URL</dt><dd><a href={entry.finalPostUrl} target="_blank" rel="noreferrer" className="break-all text-cyan-200 underline">{entry.finalPostUrl}</a></dd></div> : null}{entry.noUrlReason ? <div className="sm:col-span-2"><dt className="text-zinc-500">No-URL reason</dt><dd>{entry.noUrlReason}</dd></div> : null}</dl>{audience === "operator" && entry.metadata ? <details className="mt-3 rounded-xl border border-white/10 p-3"><summary className="cursor-pointer font-medium focus:outline-none focus:ring-2 focus:ring-cyan-200">Operational details</summary><dl className="mt-3 grid gap-2 break-all text-xs text-zinc-300 sm:grid-cols-2">{Object.entries(entry.metadata).filter(([,v])=>v!==undefined && v!==null && v!=="").map(([k,v])=><div key={k}><dt className="text-zinc-500">{k}</dt><dd>{String(v)}</dd></div>)}</dl></details> : null}</article></li>)}</ol></section>
}
