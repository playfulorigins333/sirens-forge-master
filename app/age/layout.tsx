import '../globals.css';

export const metadata = {
  title: 'Sirens Forge',
};

export default function AgeCheckLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
