"use client"
import { useMemo, useState } from "react"
import { useRouter } from "next/navigation"
import { confirmCancelledReconciliation, resolveScheduleLocalDateTime } from "@/lib/creator-publishing-queue/scheduling/validation"
import type { SafeSchedulingView } from "@/lib/creator-publishing-queue/scheduling/types"
function newKey(){ return globalThis.crypto?.randomUUID?.().replaceAll("-","_") ?? `${Date.now()}_${Math.random().toString(36).slice(2)}` }
export function Task15PlanScheduling({view}:{view:SafeSchedulingView}){ const router=useRouter(); const [selected,setSelected]=useState(view.draftPlans.find(p=>p.eligible)?.publishingPlanId??""); const [date,setDate]=useState(""); const [time,setTime]=useState(""); const [tz,setTz]=useState(Intl.DateTimeFormat().resolvedOptions().timeZone||"UTC"); const [offset,setOffset]=useState<string>(""); const [key,setKey]=useState(newKey()); const [cancelKey,setCancelKey]=useState(newKey()); const [status,setStatus]=useState(""); const [pending,setPending]=useState(false); const [cancelReason,setCancelReason]=useState(""); const plan=view.draftPlans.find(p=>p.publishingPlanId===selected); const summary=useMemo(()=>{ try{return date&&time&&tz?resolveScheduleLocalDateTime({date,time,utcOffsetMinutes:offset===""?undefined:Number(offset)},tz):null}catch(e:any){return {error:e.message,occurrences:e.occurrences} as any} },[date,time,tz,offset]); async function schedule(){ if(!plan?.platformJobId||!summary||"error" in summary) return; if(!confirm("Schedule this assisted/manual OnlyFans plan? Sirens Forge will not log in or post to OnlyFans.")) return; setPending(true); setStatus(""); try{ const r=await fetch("/api/creator-publishing-queue/scheduling/schedule",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({publishingPlanId:plan.publishingPlanId,platformJobId:plan.platformJobId,localDateTime:{date,time,utcOffsetMinutes:offset===""?undefined:Number(offset)},scheduleTimezone:tz,idempotencyKey:key})}); const b=await r.json().catch(()=>null); if(r.ok&&b?.ok){ setStatus("Schedule confirmed. Trusted state is refreshing."); setKey(newKey()); router.refresh() } else if(b?.error){ setStatus("Scheduling was not confirmed: "+(b.message||"trusted state was stale.")) } else setStatus("Scheduling outcome is unknown. Reload trusted state before starting a new action; retry this same deliberate action with the same key if needed.") }catch{ setStatus("Scheduling outcome is unknown. Reload trusted state before starting a new action; retry this same deliberate action with the same key if needed.") }finally{setPending(false)} }
 async function cancel(planId:string){ const reason=cancelReason.trim(); if(!reason) return; if(!confirm("Cancel this scheduled assisted/manual plan?")) return; setPending(true); setStatus(""); try{ const r=await fetch("/api/creator-publishing-queue/scheduling/cancel-plan",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({publishingPlanId:planId,cancellationReason:reason,idempotencyKey:cancelKey})}); const b=await r.json().catch(()=>null); if(r.ok&&b?.ok){ setStatus("Cancellation confirmed. Trusted state is refreshing."); setCancelKey(newKey()); router.refresh() } else if(confirmCancelledReconciliation(view.cancelledReconciliationPlans,planId,reason)){ setStatus("Cancellation was reconciled after refresh with the same idempotency key; no automatic retry was made.") } else if(b?.error) setStatus("Cancellation was not confirmed: "+(b.message||"trusted state was stale.")); else setStatus("Cancellation outcome is unknown. Reload trusted state; keep the same idempotency key for the same deliberate cancellation.") }catch{ setStatus("Cancellation outcome is unknown. Reload trusted state; keep the same idempotency key for the same deliberate cancellation.") }finally{setPending(false)} }
 return <section className="relative z-10 mx-auto mt-8 max-w-6xl rounded-3xl border border-violet-300/30 bg-slate-950/90 p-5 text-white"><p className="text-xs font-semibold uppercase tracking-[0.25em] text-violet-200">Task 15 trusted plan scheduling</p><h2 className="mt-2 text-2xl font-bold">Schedule one OnlyFans assisted/manual draft job</h2><p className="mt-3 text-sm text-slate-200">OnlyFans remains assisted/manual: Sirens Forge creates internal plan, job, and operator-due schedule state only. It never logs into OnlyFans, posts directly, uses OnlyFans credentials, invokes the scheduler runner, or retries automatically.</p><div className="mt-5 space-y-3">{view.draftPlans.map(p=><label key={p.publishingPlanId} className="block rounded-2xl border border-white/10 bg-white/[0.04] p-4"><input aria-label={`Select ${p.title}`} type="radio" name="schedulePlan" disabled={!p.eligible} checked={selected===p.publishingPlanId} onChange={()=>setSelected(p.publishingPlanId)} className="mr-3"/><strong>{p.title}</strong><span className="ml-2 text-sm text-slate-300">{p.eligible?"Schedulable OnlyFans assisted plan":p.reason}</span></label>)}</div><div className="mt-5 grid gap-3 md:grid-cols-3"><label>Local date<input className="mt-1 w-full rounded bg-slate-900 p-2" value={date} onChange={e=>setDate(e.target.value)} placeholder="YYYY-MM-DD"/></label><label>Local time<input className="mt-1 w-full rounded bg-slate-900 p-2" value={time} onChange={e=>setTime(e.target.value)} placeholder="HH:mm"/></label><label>IANA timezone<input className="mt-1 w-full rounded bg-slate-900 p-2" value={tz} onChange={e=>setTz(e.target.value)} /></label></div>{summary&&"error" in summary&&<div className="mt-4 rounded border border-amber-300/40 p-3 text-amber-100">Time needs attention. Fall-back ambiguity requires explicit offset occurrence.{summary.occurrences?.map((o:any)=><label key={o.intendedPublishAt} className="ml-3 inline-block"><input type="radio" name="offset" value={o.utcOffsetMinutes} onChange={e=>setOffset(e.target.value)}/> {o.utcOffsetMinutes} min · {o.intendedPublishAt}</label>)}</div>}{summary&&!("error" in summary)&&<div className="mt-4 rounded-2xl border border-cyan-300/30 bg-cyan-300/10 p-4 text-sm"><p>Confirm local: {summary.localDate} {summary.localTime} {summary.scheduleTimezone}</p><p>UTC offset: {summary.utcOffsetMinutes} minutes</p><p>Resulting UTC instant: {summary.intendedPublishAt}</p><p>Operator due occurs 60 minutes before intended publication: {summary.operatorDueLocalDate} {summary.operatorDueLocalTime} local / {summary.operatorDueAt} UTC</p></div>}<button type="button" disabled={pending||!plan?.eligible||!summary||"error" in (summary as any)} onClick={schedule} className="mt-4 rounded-xl bg-violet-300 px-5 py-3 font-semibold text-slate-950 disabled:bg-slate-700">Deliberately schedule selected plan</button><div className="mt-8"><h3 className="text-xl font-semibold">Deliberate cancellation</h3>{view.activePlans.map(p=><div key={p.publishingPlanId} className="mt-3 rounded-2xl border border-white/10 p-4"><p>{p.title} · {p.status} · OnlyFans assisted/manual cancellation only</p><label className="mt-2 block">Cancellation reason<input className="mt-1 w-full rounded bg-slate-900 p-2" value={cancelReason} onChange={e=>setCancelReason(e.target.value)} maxLength={500}/></label><button type="button" onClick={()=>cancel(p.publishingPlanId)} disabled={pending||cancelReason.trim().length===0} className="mt-3 rounded-xl border border-rose-200/60 px-4 py-2">Deliberately cancel plan schedule</button></div>)}</div>{status&&<p role="status" className="mt-4 rounded-2xl border border-white/15 bg-white/[0.06] p-4">{status}</p>}</section> }
