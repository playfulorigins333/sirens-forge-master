import { NextResponse } from "next/server"
import { ensureActiveSubscription } from "@/lib/subscription-checker"
import { autopostSubscriptionGateStatus } from "@/lib/creator-publishing-queue/autopost/routeGate"
import { AUTOPOST_SCHEMA_UNAVAILABLE, loadAutopostCapabilities } from "@/lib/creator-publishing-queue/autopost/service"
export async function GET(){ const auth=await ensureActiveSubscription(); if(!auth.ok) return NextResponse.json({error:auth.error ?? "SUBSCRIPTION_REQUIRED"},{status:autopostSubscriptionGateStatus(auth.error, auth.status)}); try{ return NextResponse.json({capabilities: await loadAutopostCapabilities()}) }catch(e:any){ if(e?.code === "UNAUTHENTICATED" || e?.message === "UNAUTHENTICATED") return NextResponse.json({error:"UNAUTHENTICATED"},{status:401}); if(e?.code === AUTOPOST_SCHEMA_UNAVAILABLE) return NextResponse.json({error:AUTOPOST_SCHEMA_UNAVAILABLE,message:"Autopost orchestration is temporarily unavailable."},{status:503}); return NextResponse.json({error:"AUTOPOST_CAPABILITIES_UNAVAILABLE"},{status:500}) } }
