import { NextRequest,NextResponse } from "next/server"; import { ensureActiveSubscription } from "@/lib/subscription-checker"; import { getSupabaseAdmin } from "@/lib/supabaseAdmin"; import { isVideoSourceUploadReady,promoteSource,signStagingUpload,validateSourceMetadata,VIDEO_SOURCE_UPLOAD_TTL_SECONDS } from "@/lib/video/sourceUpload"; import { UUID_RE } from "@/lib/video/contract";
export const runtime="nodejs";export const dynamic="force-dynamic";const headers={"Cache-Control":"no-store"};
export async function POST(req:NextRequest){const auth=await ensureActiveSubscription();if(!auth.ok||!auth.user)return NextResponse.json({error:auth.error,message:auth.message},{status:auth.status,headers});if(!isVideoSourceUploadReady())return NextResponse.json({error:"VIDEO_SOURCE_UPLOAD_UNAVAILABLE"},{status:503,headers});try{const body=await req.json(),mime=String(body.content_type||""),size=Number(body.size_bytes);validateSourceMetadata(mime,size);const signed=await signStagingUpload({ownerId:auth.user.id,mime,size});const {error}=await getSupabaseAdmin().rpc("create_video_source_upload",{p_upload_id:signed.uploadId,p_owner_id:auth.user.id,p_staging_bucket:signed.bucket,p_staging_key:signed.stagingKey,p_final_bucket:signed.bucket,p_final_key:signed.finalKey,p_expected_mime:mime,p_expected_size:size,p_expires_at:new Date(Date.now()+VIDEO_SOURCE_UPLOAD_TTL_SECONDS*1000).toISOString()});if(error)throw error;return NextResponse.json({upload_id:signed.uploadId,upload_url:signed.uploadUrl,content_type:mime,expires_in:VIDEO_SOURCE_UPLOAD_TTL_SECONDS},{status:201,headers});}catch{return NextResponse.json({error:"INVALID_VIDEO_SOURCE_UPLOAD"},{status:400,headers});}}
export async function PATCH(req:NextRequest){const auth=await ensureActiveSubscription();if(!auth.ok||!auth.user)return NextResponse.json({error:auth.error,message:auth.message},{status:auth.status,headers});try{const body=await req.json(),id=String(body.upload_id||"");if(!UUID_RE.test(id))throw new Error();const admin=getSupabaseAdmin();const {data,error}=await admin.rpc("get_video_source_upload",{p_upload_id:id,p_owner_id:auth.user.id});if(error)throw error;const row=Array.isArray(data)?data[0]:data;if(!row)return NextResponse.json({error:"NOT_FOUND"},{status:404,headers});if(row.generation_asset_id)return NextResponse.json({upload_id:id,generation_id:row.generation_id,generation_asset_id:row.generation_asset_id},{headers});const verified=await promoteSource({stagingKey:row.staging_key,finalKey:row.final_key,mime:row.expected_mime_type,size:Number(row.expected_size_bytes)});const finalized=await admin.rpc("finalize_video_source_upload",{p_upload_id:id,p_owner_id:auth.user.id,p_mime_type:verified.mimeType,p_size_bytes:verified.sizeBytes,p_sha256:verified.sha256});if(finalized.error)throw finalized.error;void verified.cleanup().catch(()=>{});const result=Array.isArray(finalized.data)?finalized.data[0]:finalized.data;return NextResponse.json({upload_id:id,generation_id:result.generation_id,generation_asset_id:result.generation_asset_id},{headers});}catch{return NextResponse.json({error:"VIDEO_SOURCE_FINALIZE_UNAVAILABLE"},{status:503,headers});}}
