export function normalizeHistoryTimezone(tz?: string | null){ const value=(tz ?? "").trim(); if(!value) return "UTC"; try { new Intl.DateTimeFormat("en-US",{timeZone:value}).format(new Date("2026-01-01T00:00:00Z")); return value } catch { return "UTC" } }
export function formatHistoryTimestamp(iso:string, timezone:string){ const tz=normalizeHistoryTimezone(timezone); const d=new Date(iso); const parts=new Intl.DateTimeFormat("en-US",{timeZone:tz,year:"numeric",month:"short",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false,timeZoneName:"short"}).formatToParts(d); const get=(t:string)=>parts.find(p=>p.type===t)?.value ?? ""; return `${get("month")} ${get("day")}, ${get("year")} ${get("hour")}:${get("minute")}:${get("second")} ${get("timeZoneName")}` }
