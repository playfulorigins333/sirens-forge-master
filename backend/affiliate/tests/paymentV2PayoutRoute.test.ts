import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{payoutExecutionResponse}from"../../../app/api/admin/affiliate-payouts/execute/route";const old={...process.env};process.env.PAYMENT_V2_PAYOUT_EXECUTION_ENABLED="true";process.env.CRON_SECRET="secret";let calls=0;const order:string[]=[];const deps:any={prepare:async()=>{order.push("prepare");return"batch"},loadPending:async()=>{order.push("load");return[]},reconcileRecurring:async()=>true,beginDispatch:async()=>null,createTransfer:async()=>{calls++;return{id:"tr_x"}},complete:async()=>true,recordFailure:async()=>{}};assert.equal((await payoutExecutionResponse(new Request("http://x",{method:"POST"}),deps)).status,401);assert.equal(calls,0);assert.equal((await payoutExecutionResponse(new Request("http://x",{method:"POST",headers:{authorization:"Bearer secret"}}),deps)).status,200);assert.deepEqual(order,["prepare","load"]);const source=readFileSync("app/api/admin/affiliate-payouts/execute/route.ts","utf8");assert.match(source,/result\.status===200&&result\.body\.status==="received"/,"payout-time recurring proof requires received, not ignored");process.env=old;console.log("PFC-CORE-03D payout route passed (4 assertions).")
