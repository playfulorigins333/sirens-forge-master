import test from "node:test";import assert from "node:assert/strict";import {GET} from "../../../app/api/internal/notifications/phase9/run/route"
test("scheduler rejects unauthenticated requests before delivery",async()=>{process.env.CRON_SECRET="test-secret";delete process.env.PHASE9_NOTIFICATIONS_ENABLED;const response=await GET(new Request("https://example.test/api/internal/notifications/phase9/run"));assert.equal(response.status,401);assert.deepEqual(await response.json(),{ok:false,error:"UNAUTHORIZED"})})
test("authenticated scheduler is inert while exact gate is off",async()=>{process.env.CRON_SECRET="test-secret";process.env.PHASE9_NOTIFICATIONS_ENABLED="TRUE";const response=await GET(new Request("https://example.test/api/internal/notifications/phase9/run",{headers:{authorization:"Bearer test-secret"}}));assert.equal(response.status,200);assert.deepEqual(await response.json(),{ok:true,enabled:false})})
