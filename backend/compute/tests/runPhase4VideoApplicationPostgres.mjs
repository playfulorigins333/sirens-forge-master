import {spawnSync} from "node:child_process";
const url=process.env.COMPUTE_JOB_PLANE_DATABASE_URL;if(!url){console.error("COMPUTE_JOB_PLANE_DATABASE_URL is required");process.exit(2)}
const run=(args)=>{const r=spawnSync("psql",[url,"-v","ON_ERROR_STOP=1",...args],{encoding:"utf8"});if(r.status){process.stderr.write(r.stderr);process.stdout.write(r.stdout);process.exit(r.status??1)}};
for(const file of ["backend/compute/tests/computeJobPlanePostgresSetup.sql","supabase/migrations/20260824090000_private_creator_generation_media.sql","supabase/migrations/20260825090000_durable_compute_job_plane.sql","supabase/migrations/20260826004344_durable_compute_pass_4a_finalization.sql","supabase/migrations/20260826120000_durable_compute_pass_4c_video_stitch_foundation.sql","supabase/migrations/20260831120000_phase4_video_subscriber_application.sql"])run(["-f",file]);
run(["-c",`do $$ begin assert (select is_nullable='YES' from information_schema.columns where table_schema='public' and table_name='video_projects' and column_name='identity_id'); assert to_regclass('public.video_source_uploads') is not null; assert (select relrowsecurity and relforcerowsecurity from pg_class where oid='public.video_source_uploads'::regclass); assert to_regprocedure('public.submit_video_project_compute_jobs(uuid,uuid,uuid,text,text,jsonb,text)') is not null; assert to_regprocedure('public.finalize_video_source_upload(uuid,uuid,text,bigint,text)') is not null; assert not has_table_privilege('service_role','public.video_source_uploads','select'); end$$;`]);
console.log("Phase 4 Video application PostgreSQL integration passed.");
