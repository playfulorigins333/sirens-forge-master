import { spawnSync } from "node:child_process";
const url=process.env.COMPUTE_JOB_PLANE_DATABASE_URL;if(!url){console.error("COMPUTE_JOB_PLANE_DATABASE_URL is required");process.exit(2)}
const run=(args)=>{const r=spawnSync("psql",[url,"-v","ON_ERROR_STOP=1",...args],{stdio:"inherit"});if(r.status)process.exit(r.status??1)};
for(const f of ["backend/compute/tests/computeJobPlanePostgresSetup.sql","supabase/migrations/20260824090000_private_creator_generation_media.sql","supabase/migrations/20260825090000_durable_compute_job_plane.sql","supabase/migrations/20260826004344_durable_compute_pass_4a_finalization.sql"])run(["-f",f]);
run(["-c",`create function auth.uid() returns uuid language sql stable as $$select nullif(current_setting('request.jwt.claim.sub',true),'')::uuid$$; alter table public.user_loras enable row level security; alter table public.user_loras add column name text,add column description text;
create table public.dataset_doctor_jobs(id uuid primary key,lora_id uuid not null,user_id uuid not null,status text not null,final_r2_bucket text,final_r2_prefix text,summary jsonb not null);create table public.dataset_doctor_images(id uuid primary key,job_id uuid,user_id uuid not null);create table public.dataset_doctor_selections(id uuid primary key,job_id uuid not null,image_id uuid not null,user_id uuid not null,selection_type text not null);alter table public.dataset_doctor_jobs enable row level security;alter table public.dataset_doctor_images enable row level security;alter table public.dataset_doctor_selections enable row level security;grant select,insert,update,delete on all tables in schema public to anon,authenticated,service_role;
create policy old_lora_all on public.user_loras for all to authenticated using(auth.uid()=user_id) with check(auth.uid()=user_id);create policy old_jobs_all on public.dataset_doctor_jobs for all to authenticated using(auth.uid()=user_id) with check(auth.uid()=user_id);create policy old_images_all on public.dataset_doctor_images for all to authenticated using(auth.uid()=user_id) with check(auth.uid()=user_id);create policy old_selections_all on public.dataset_doctor_selections for all to authenticated using(auth.uid()=user_id) with check(auth.uid()=user_id);`]);
run(["-f","supabase/migrations/20260827050727_trainer_dataset_authority_lock.sql"]);run(["-f","backend/compute/tests/trainerDatasetAuthorityPostgresIntegration.sql"]);console.log("Trainer/Dataset authority PostgreSQL integration passed.");
