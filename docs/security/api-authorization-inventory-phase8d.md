# API authorization inventory — Phase 8D/8E retention extensions

This file is an additive extension to the existing API authorization inventory for the Phase 8D canceled-account and Phase 8E subscription-delinquency retention runners.

**Inventory extension:** 2 route files / 2 route-method entries
**Combined inventory:** 123 route files / 145 route-method entries

| Route path | Source file | Method | Caller class | Authorization class | Authentication mechanism | Ownership boundary | Entitlement boundary | Admin/operator boundary | External signature/secret boundary | Privileged client/service-role usage | Validation/safe-error notes | Reviewed status | Evidence/test reference |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `/api/internal/retention/phase8d/run` | `app/api/internal/retention/phase8d/run/route.ts` | `GET` | Scheduled internal retention caller | SCHEDULER_SECRET + INTERNAL_CONTROLLED | Shared `authenticateSchedulerRequest()` validates `CRON_SECRET` or `VERCEL_CRON_SECRET` via timing-safe comparison before retention work | Caller supplies no creator/account selector; eligible cancellation records are selected only by the server/database retention contract and every asset/Twin operation remains owner-bound | Not entitlement-gated because this is a system retention/compliance operation; creator read access already expires at `retention_until` | No browser/admin surface; scheduled internal control only | Bearer or Vercel cron secret; missing, malformed, or invalid secret fails closed before privileged work | Server-only service uses the Supabase admin client only after scheduler authentication; database claims are bounded and existing private-media/Twin purge authorities remain responsible for physical deletion | GET has no body or caller-selected IDs; response is bounded and `no-store`; legal holds block destructive work without extending creator read access; Auth, billing evidence, delinquency, voluntary-deletion purge, and Phase 9 delivery are outside this route | PASS | `app/api/internal/retention/phase8d/run/route.ts`; `lib/retention/phase8d.ts`; `backend/security/tests/phase8dCanceledAccountEnforcementSource.test.ts`; inventory contract test |
| `/api/internal/retention/phase8e/run` | `app/api/internal/retention/phase8e/run/route.ts` | `GET` | Scheduled internal retention caller | SCHEDULER_SECRET + INTERNAL_CONTROLLED | Shared `authenticateSchedulerRequest()` validates `CRON_SECRET` or `VERCEL_CRON_SECRET` via timing-safe comparison before delinquency-retention work | Caller supplies no creator/account selector; only due `retention_countdown` delinquency records are database-selected; asset/Twin operations remain owner-bound and cutoff-bound | Not entitlement-gated because this is a system compliance operation; Phase 7 already freezes creator tools on delinquency while billing recovery, privacy, security, and data-rights surfaces remain available | No browser/admin surface; scheduled internal control only | Bearer or Vercel cron secret; missing, malformed, or invalid secret fails closed before privileged work | Server-only service uses the Supabase admin client only after scheduler authentication; claim/validate/finalize RPCs are service-role only and existing physical-purge authorities remain storage executors | GET has no body or caller-selected IDs; response is bounded and `no-store`; recovery/new billing supersedes old purge authority; legal holds fail closed; delinquency/invoice/Auth/profile/billing/governance evidence is preserved; Phase 9 delivery remains outside this route | PASS | `app/api/internal/retention/phase8e/run/route.ts`; `lib/retention/phase8e.ts`; `backend/security/tests/phase8eSubscriptionDelinquencyEnforcementSource.test.ts`; PostgreSQL integration; inventory contract test |
