# API authorization inventory — Phase 8F legal-hold lifecycle extension

This file extends the existing API authorization inventory for Founder/Admin legal-hold review controls and the internal expiry reconciler.

**Inventory extension:** 4 route files / 5 route-method entries
**Combined inventory:** 127 route files / 150 route-method entries

| Route path | Source file | Method | Caller class | Authorization class | Authentication mechanism | Ownership boundary | Entitlement boundary | Admin/operator boundary | External signature/secret boundary | Privileged client/service-role usage | Validation/safe-error notes | Reviewed status | Evidence/test reference |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `/api/admin/governance/legal-holds` | `app/api/admin/governance/legal-holds/route.ts` | `GET` | Founder/Admin | AUTHENTICATED + FRESH_TOTP + ADMIN | `requireAdminCapability("governance.legal_hold.manage")` verifies the Supabase server user, AAL2, verified TOTP factor, and authoritative TOTP AMR timestamp within 10 minutes; database RPC independently verifies the current Founder/Admin authority bridge | Caller cannot select another acting admin identity; actor UUID comes from the authenticated server session; returned holds remain subject-scoped by immutable hold targets | Not subscription-entitlement gated because legal-hold review is a governance/compliance function | Database independently requires `governance_actor_is_founder_admin()`; Phase 10 capability is required at HTTP and founder authority remains independently checked by the database | None | `getSupabaseAdmin()` is used only after fresh-TOTP authentication; direct service-role SELECT on legal-hold tables is revoked and `list_governance_legal_holds_for_admin` performs the read and appends an audit event | Only `status` and `limit` query keys are accepted; status is enumerated and limit is 1–100; response is `private, no-store`; raw database errors are not returned | PASS | `lib/governance/legalHolds.ts`; `supabase/migrations/20260905110000_phase8f_legal_hold_lifecycle.sql`; Phase 8F source/PostgreSQL tests |
| `/api/admin/governance/legal-holds` | `app/api/admin/governance/legal-holds/route.ts` | `POST` | Founder/Admin | AUTHENTICATED + FRESH_TOTP + ADMIN | `requireAdminCapability("governance.legal_hold.manage")` runs before privileged access and supplies the authoritative fresh TOTP AMR timestamp; database open RPC independently rechecks Founder/Admin and <=10-minute TOTP freshness | Actor UUID comes only from session; target subject UUIDs are validated server-side and account targets must use `subject_user_id` as canonical target ID | Not subscription-entitlement gated; preservation authority is governance/compliance scoped | Founder/Admin-only database RPC; finite review/expiry dates and scoped targets required | `Idempotency-Key` header required and bounded to the database idempotency contract | Server-only Supabase admin RPC invocation occurs after authentication; no direct hold-table mutation from the route | Strict body keys, bounded text/target counts, UUID/date validation, safe error codes, `private, no-store`; no notification delivery | PASS | `lib/governance/legalHolds.ts`; `open_governance_legal_hold`; Phase 8F source/PostgreSQL tests |
| `/api/admin/governance/legal-holds/[holdId]/review` | `app/api/admin/governance/legal-holds/[holdId]/review/route.ts` | `POST` | Founder/Admin | AUTHENTICATED + FRESH_TOTP + ADMIN | `requireAdminCapability("governance.legal_hold.manage")` verifies AAL2/fresh TOTP and database review RPC independently repeats Founder/Admin and fresh-TOTP validation | Hold ID is UUID-validated; actor identity is session-derived; review cannot retarget immutable hold targets | Not subscription-entitlement gated | Founder/Admin-only review; preservation expiry may stay equal or extend but cannot shorten; only active non-expired holds are reviewable | `Idempotency-Key` required; no external provider/signature | Server-only service-role RPC after authentication; immutable review evidence and audit event are written transactionally | Strict JSON body; future review/expiry bounds; shortening and lifecycle conflicts return safe conflict responses; no raw DB error | PASS | `review_governance_legal_hold`; `governance_legal_hold_reviews`; Phase 8F source/PostgreSQL tests |
| `/api/admin/governance/legal-holds/[holdId]/release` | `app/api/admin/governance/legal-holds/[holdId]/release/route.ts` | `POST` | Founder/Admin | AUTHENTICATED + FRESH_TOTP + ADMIN | `requireAdminCapability("governance.legal_hold.manage")` verifies fresh TOTP before service-role use; database release RPC independently rechecks Founder/Admin/fresh TOTP | Hold ID is UUID-validated and release cannot alter immutable target scope | Not subscription-entitlement gated | Only Founder/Admin may explicitly release an active non-time-expired hold; elapsed holds must be reconciled as system expiry rather than mislabeled manual release | `Idempotency-Key` required; no external provider/signature | Server-only service-role release RPC after authentication; release evidence and audit event remain database-authoritative | Strict release-reason body; not-found/lifecycle/idempotency conflicts are safe-coded; response is `private, no-store` | PASS | `release_governance_legal_hold`; Phase 8F source/PostgreSQL tests |
| `/api/internal/governance/legal-holds/expire` | `app/api/internal/governance/legal-holds/expire/route.ts` | `GET` | Scheduled internal governance caller | SCHEDULER_SECRET + INTERNAL_CONTROLLED | Shared `authenticateSchedulerRequest()` validates `CRON_SECRET` or `VERCEL_CRON_SECRET` before any privileged work | Caller supplies no hold IDs or creator IDs; database selects only active holds whose finite expiry has elapsed, with bounded `SKIP LOCKED` processing | Not entitlement-gated; this is lifecycle bookkeeping and does not restore or extend creator access | No browser/admin surface; system expiry is distinct from Founder/Admin explicit release/review | Bearer/Vercel cron secret; invalid or absent secret fails closed before service-role use | Supabase admin client is reached only after scheduler authentication; `phase8f_expire_governance_legal_holds` is service-role-only and writes system audit evidence | Fixed batch limit 50; no request body/query selectors; `private, no-store`; Phase 9 notification delivery is not invoked | PASS | `app/api/internal/governance/legal-holds/expire/route.ts`; `phase8f_expire_governance_legal_holds`; Phase 8F source/PostgreSQL tests |
