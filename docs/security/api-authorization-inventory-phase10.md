# Phase 10 API authorization inventory

This is the Phase 10 delta to the canonical inventory. Sensitive responses use `Cache-Control: private, no-store`. Scheduler-operated `/api/admin/*` routes and X provider authorization are not reclassified.

| Route | File | Method | Caller | Authorization | Session/auth mechanism | Ownership boundary | Entitlement | Capability | Sensitive dependency | Privileged access | Validation and safe errors | Status | Review |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `/api/admin/governance/audit-events` | `app/api/admin/governance/audit-events/route.ts` | `GET` | Human governance/security administrator | AUTHENTICATED + FRESH_TOTP + ADMIN | Verified Supabase server session; database rechecks actor capability | Actor UUID only from session; no user lookup | Not required | `governance.audit.read` | None | Server-only bounded audited RPC; no direct audit SELECT | Sequence cursor, limit 1–100, finite filters, minimized columns; successful read appends minimized read evidence | PASS | Phase 10 audit-read contract |
| `/api/account/support/cases` | `app/api/account/support/cases/route.ts` | `GET` | Authenticated creator | AUTHENTICATED + OWNER | Verified Supabase server session; RPC derives `auth.uid()` | Only caller's support cases | Not required | None | None | Authenticated bounded RPC; no service role | Composite timestamp + UUID cursor and limit 1–50; sanitized no-store response | PASS | Phase 10 creator-support ownership boundary |
| `/api/account/support/cases` | `app/api/account/support/cases/route.ts` | `POST` | Authenticated creator | AUTHENTICATED + OWNER | Verified Supabase server session; RPC derives `auth.uid()` | New case always bound to caller | Not required | None | None | Authenticated bounded RPC; no service role | Exact body, finite category, summary 3–500; sanitized errors | PASS | Phase 10 creator-support ownership boundary |
| `/api/admin/support/cases` | `app/api/admin/support/cases/route.ts` | `GET` | Human support administrator | AUTHENTICATED + FRESH_TOTP + ADMIN | Verified Supabase server session; database rechecks actor capability | Actor UUID only from session; bounded queue metadata | Not required | `support.case.read` | None | Server-only bounded queue RPC | Finite state, composite timestamp + UUID cursor, limit 1–50 | PASS | Phase 10 support queue |
| `/api/admin/support/cases/[caseId]` | `app/api/admin/support/cases/[caseId]/route.ts` | `PATCH` | Human support administrator | AUTHENTICATED + FRESH_TOTP + ADMIN | Verified Supabase server session; database rechecks actor capability | Actor UUID never accepted from body or path | Not required | `support.case.manage` | None | Server-only lifecycle RPC | UUID, finite transition, bounded note; generic conflict | PASS | Phase 10 lifecycle and audit boundary |
