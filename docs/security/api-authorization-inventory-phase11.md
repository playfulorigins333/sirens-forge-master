# Phase 11 API authorization inventory

**Inventory extension:** 4 route files / 5 route-method entries  
**Combined inventory:** 136 route files / 161 route-method entries

| Route | File | Method | Caller | Authorization | Session/auth mechanism | Ownership boundary | Entitlement | Capability | Sensitive dependency | Privileged access | Validation and safe errors | Status | Review |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `/api/age-attestation` | `app/api/age-attestation/route.ts` | `POST` | Any visitor | PUBLIC | None; server issues only an attestation cookie | No identity or account claim | Not required | None | None | No database or service role | Exact form content type/body/field; validated local return path; HttpOnly cookie; no-store | PASS | Phase 11 age self-attestation |
| `/api/safety/reports` | `app/api/safety/reports/route.ts` | `POST` | Any safety/legal reporter | PUBLIC | Anonymous; optional contact is report data, not authentication | Creates one new case and cannot read/enumerate cases | Not required | None | None | Server-only service role invokes create-only RPC; never exposed | Exact JSON, 16 KiB, allowlisted bounded fields, finite values, no uploads, generic no-store errors | PASS | Phase 11 public intake |
| `/api/admin/safety/cases` | `app/api/admin/safety/cases/route.ts` | `GET` | Human trust/safety administrator | AUTHENTICATED + FRESH_TOTP + ADMIN | Verified Supabase server session and fresh TOTP; database rechecks capability | Session-derived actor; queue exposes bounded case metadata | Not required | `safety.case.read` | None | Server-only service role invokes capability-checked audited RPC | Finite state, deterministic timestamp/UUID cursor, limit 1–50, no-store | PASS | Phase 11 safety queue |
| `/api/admin/safety/cases/[caseRef]` | `app/api/admin/safety/cases/[caseRef]/route.ts` | `GET` | Human trust/safety administrator | AUTHENTICATED + FRESH_TOTP + ADMIN | Verified Supabase server session and fresh TOTP; database rechecks capability | One validated public case reference | Not required | `safety.case.read` | None | Server-only service role invokes capability-checked audited RPC | Exact reference; generic not-found; no-store | PASS | Phase 11 restricted case read |
| `/api/admin/safety/cases/[caseRef]` | `app/api/admin/safety/cases/[caseRef]/route.ts` | `PATCH` | Human trust/safety administrator | AUTHENTICATED + FRESH_TOTP + ADMIN | Verified Supabase server session and fresh TOTP; database rechecks capability | One validated public case reference; actor never client supplied | Not required | `safety.case.manage` | None | Server-only service role invokes finite transition RPC | Exact JSON, finite state/reason, bounded text, generic conflict, no-store | PASS | Phase 11 state machine |
